name: Run Examples
on:
  workflow_call:
    inputs:
      sdk_version:
        description: 'SDK version to run examples for'
        required: false
        type: string
        default: 'LATEST'
      jdk:
        description: 'JDK version to use'
        required: true
        type: string
        default: '17'
      sdk_generation_workflow_run_id:
        description: 'ID of the SDK generation workflow run'
        required: false
        type: string
        default: ''
    secrets:
      KEY:
        description: 'API Key'
        required: true
      SECRET:
        description: 'API Key'
        required: true

jobs:
  run-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: 'ExpediaGroup/rapid-java-sdk'
          ref: 'OmarAlJarrah/add-cli-execution-plugin'
      - uses: actions/download-artifact@v4
        with:
          name: jar
          path: jar
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ inputs.jdk }}
          distribution: 'temurin'
      - name: Install SDK Jar
        run: ls -R jar && mvn install:install-file -Dfile=jar/*.jar -DgroupId=com.expediagroup -DartifactId=rapid-sdk -Dversion=${{inputs.sdk_version}} -Dpackaging=jar
      - name: Run Examples
        working-directory: examples
        run: mvn clean install exec:java -Drapid-java-sdk.sdk.version="${{inputs.sdk_version}}-SNAPSHOT" -Dcom.expediagroup.rapidsdkjava.apikey="${{secrets.KEY}}" -Dcom.expediagroup.rapidsdkjava.apisecret="${{secrets.SECRET}}"
